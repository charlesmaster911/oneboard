# oneboard 프론트엔드 환경변수 예시
# 정적 사이트이므로 실제 .env는 없음.
# 아래 변수는 index.html <script> 블록에서 window.* 로 설정합니다.

# oneboard-server API URL (배포 시 index.html에 window.API_BASE 로 삽입)
# 예: window.API_BASE = 'https://oneboard-server.onrender.com/api'
API_BASE=https://oneboard-server.onrender.com/api
